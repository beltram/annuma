#[derive(Debug, Copy, Clone, clap::ValueEnum, strum::Display)]
pub enum LoireAtlantique {
    Guerande,
    Vallet,
    SaintNazaire,
}
#[derive(Debug, Copy, Clone, clap::ValueEnum, strum::Display)]
pub enum Mayenne {}
